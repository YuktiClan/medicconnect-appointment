server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fhir_db?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: passw0rd
  flyway:
    enabled: false
  eureka:
    client:
      register-with-eureka: true
      fetch-registry: true
      service-url:
        defaultZone: http://localhost:8761/eureka/

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
  data:
    redis:
      host: localhost
      port: 6379
  cache:
    redis:
      time-to-live: 15m

hapi:
  fhir:
    fhir_version: R4
    allow_external_references: true
    allow_multiple_delete: true
google:
  client-id: 893832632080-jb16kdcif93cfh3mbfsv5e7bhq93n78b1.apps.googleusercontent.com
  client-secret: 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1
  redirect-uri: http://localhost:8081/api/google/callback
